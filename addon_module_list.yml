modules :
  addon_modules :
    author : sasaki2
    author_email : hoge@hoge.com
    name :
      default : addon modules
      en : Addon modules
      ja : アドオンモジュール
    summary : 
      default : |
        Addon module for zencart.
      ja : |
        ZenCart用のモジュール管理プログラム
    version : 1.0.2
    require_zen_cart_version : >= 1.3.0.2
    require_addon_modules_version :
  aboutbox :
    author : s.saito
    author_email : saito@ark-web.jp
    name :
      default : about_box
      en : About Box
      ja : アバウトボックス
    summary : 
      default : |
        About Box block.
        add block for About Box.
      ja : |
        アバウトボックスブロック
        アバウトボックスを表示するブロックを追加します。
        有効化後にブロックの設定から表示設定をしてください。
    version : 1.0.3
    require_zen_cart_version : >= 1.3.0.2
    require_addon_modules_version : >= 1.0
  jquery :
    author : s.saito
    author_email : saito@ark-web.jp
    name :
      default : jquery
      en : jquery
      ja : jquery
    summary : 
      default : |
        jquery
        need for javascript.
      ja : |
        jquery
        javascriptで必要となります
    version : 1.0.3
    require_zen_cart_version : >= 1.3.0.2
    require_addon_modules_version : >= 1.0
